name: Build Trading Trainer

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Download repository
        uses: actions/checkout@v6

      - name: Check project
        run: |
          echo "DJ Alpha Trading Trainer"
          echo "Build workflow is working!"
